n = int(input())
tasks = []
for _ in range(n):
    line = input().split()
    task_name = line[0]
    deadline = int(line[1])
    tasks.append((deadline, task_name))
tasks.sort()
for item in tasks:
    print(str(item[0]) + " " + item[1])
